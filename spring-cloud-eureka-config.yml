spring:
  profiles:
    active: dev
---
server:
  port: 9001
spring:
  application:
    name: spring-cloud-eureka-config-4404
  profiles: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:4404/eureka
    register-with-eureka: false
    fetch-registry: false
